<%= form_with(model: city, local: true) do |form| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
        <% city.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div align="center">
    <div class="col-5">
      <div class="card card-default">
        <div class="card-body">
          <div class="row">
            <div class="card-body height_div">
              <div class="form-group text-left">
                <label>Ciudad </label>
                <%= form.text_field :name, class: "form-control", required: "required" %>
              </div>
              <div class="form-group text-left">
                <label>Departamento </label>
                <%#= form.collection_select(:departament_id, Departament.all, :id, :name, {:prompt => 'Departamento'}, {required: true}) %>
                <%= form.select :departament_id,
                                options_from_collection_for_select(Departament.all, "id", "name", form.object.departament_id),{prompt: 'Seleccionar'},
                                {class: 'form-control', required: 'required'} %>
              </div>
            </div>
          </div>
        </div>
      </div>
      <fieldset>
        <div class="form-group row">
          <%= link_to 'Atras', cities_path, class: "btn btn-secondary" %>
          <%= form.submit 'Guardar', class: "btn btn-primary" %>
        </div>
      </fieldset>
    </div>
  </div>
<% end %>
