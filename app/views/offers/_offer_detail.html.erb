<fieldset style="margin: 0; border: 0; padding: 0;">
  <table class="table">
    <tbody>
      <tr class="row">
        <td class="col-7">
          <%= f.select :room_id,
                       options_from_collection_for_select(Room.rooms_published_by_estate(partial_param.estate_id), "id", "description", f.object.room_id), {prompt: "Seleccionar"},
                       {class: 'form-control', required: 'true'}%>
        </td>
        <td class="col-3">
          <div class="input-group">
            <%= f.number_field :discount, class: "form-control number", required: "required", type: "text", onkeypress:"event.charCode >= 48 && event.charCode <= 57", size:"3"%>
            <div class="input-group-prepend">
              <div class="percent_input">%</div>
            </div>
          </div>
        </td>
        <td class="col-2">
          <%= link_to "Eliminar" do %>
            <div class=" remove_fields2 btn btn-link text-danger">
              <i class="fas fa-trash-alt"></i>
            </div>
          <% end %>
        </td>
      </tr>
    </tbody>
  </table>
</fieldset>