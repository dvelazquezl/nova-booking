<% js = escape_javascript(
  render(
    partial: 'bookings/list_user_bookings',
    locals: {bookings: bookings, filterrific: filterrific}
  )
) %>
$("#results").html("<%= js %>");
$('.pagination a').attr('data-remote', 'true');