<fieldset style="margin: 0; border: 0; padding: 0;">

  <div class="row ocultar">
    <div class="col-md-6">
      <div class="card-body">
        <div class="form-group">
          <label>Tipo de habitacion</label>
          <%= f.select :room_type, Room.room_type.options, class: "form-control", required: "required" %>
        </div>
      </div>
      <div class="card-body">
        <div class="form-group">
          <label>Capacidad</label>
          <%= f.text_field :capacity, class: "form-control", required: "required",  placeholder: "¿Cuántos invitados pueden quedarse en esta habitación?"%>
        </div>
      </div>
      <div class="card-body">
        <div class="form-group">
          <label>Cantidad</label>
          <%= f.text_field :quantity, class: "form-control", required: "required", placeholder: "Numero de habitaciones (de este tipo)" %>
        </div>
      </div>
    </div>


    <div class="col-md-6">
      <div class="card-body">
        <div class="form-group">
          <label>Nombre de la habitacion</label>
          <%= f.text_field :description, class: "form-control room_name", required: "required" %>
        </div>
      </div>
      <div class="card-body">
        <div class="form-group">
          <label>Precio</label>
          <%= f.text_field :price, class: "form-control", required: "required" %>
        </div>
      </div>
    </div>

  </div>

  <%= link_to "Eliminar" do%>
    <div class=" remove_fields btn btn-link text-danger">
      <i class="fas fa-trash-alt"></i>
    </div>
  <%end %>

  <p class="description"></p>
  <button type="button" class="show_fields">fade in </button>
  <button type="button" class="hide_fields">fade out </button>

</fieldset>